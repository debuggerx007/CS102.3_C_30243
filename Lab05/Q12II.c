#include <stdio.h>
int main()
{
    int num, sum;
    while(num!=-1);
    {
        printf("Enter a number: ");
        scanf("%d", &num);
        sum = sum+num;
    }
    printf("Total : ", sum);
}
