#include <stdio.h>
int main()
{
    int num, digit, armstrong = 0, expo, original;
    printf("Enter a Number: ");
    scanf("%d", &num);
    for(original=num;original!=0;original/=10) //calculate the how many nos
    {
        expo++;//expo
    }
    for(original=num;original!=0;original/=10)
    {

    }
    printf("%d", expo);
}
